# The Maze RPG — State of the Project & Implementation Plan

**Last updated:** 2026-07-24
**North star:** the "dream state" is [Game Idea.md](Game%20Idea.md), with supporting numbers/systems from [Levels and Stats.xlsx](Levels%20and%20Stats.xlsx) and the class/race evolution material in [Game Class and Stats.drawio.xml](Game%20Class%20and%20Stats.drawio.xml). **As of 2026-07-24, the long-term vision expands beyond the dungeon** — see "Dungeon-as-Trial + Overworld" below, which is now the guiding frame for everything past Phase 1.
**Accepted for now:** fully automatic (real-time) movement and combat stays the *default* feel. A player-toggleable turn-based mode is planned as an additive precision tool (see below), not a wholesale control-model conversion. Manual/turn-based-always is not the direction.
**Parked:** Perlin-noise enemy wandering. The `PerlinNoise` class math is correct; the problem was the consumer (raw noise-driven heading with no wall-awareness makes enemies drift into walls in tight corridors). BFS-waypoint wandering replaced it. Leave parked.

This document is the single source of truth for *where the code is* versus *where the design wants to go*, and a dependency-ordered plan to close the gap. File references use `path:line` form.

---

## 0a. North-star pivot (2026-07-24): Dungeon-as-Trial + Overworld

The owner's long-term goal, discussed with reference to Cataclysm: Dark Days Ahead and Dwarf Fortress (Adventure mode), and independently corroborated by an architecture proposal drafted with ChatGPT: give the player genuine open-ended freedom — mine ore, become a merchant with a stall, become a mage-scholar inventing spells, tear down a house and rebuild it — via a **small set of universal systems whose interactions create freedom**, rather than scripting each lifestyle individually. Roles (miner, merchant, scholar) should emerge from capability (tools, skill, knowledge, standing) rather than being locked classes.

**Decision: TheMazeRPG stays the base**, not the SimpleRPG prior attempt (see the Prior-Attempt Reference section below for the comparison that led here). Reasoning, briefly: `GameState` is already headless/UI-decoupled (every `TEST_*` demo proves this), we already have a working `Combinable`/`CombinationEngine` merge system, a stable-id content-registry seed (`classes.json`/`races.json`/`Attack.Id`), and a typed/tooled language — all of which SimpleRPG lacks and would need to be rebuilt from zero regardless of which codebase "wins." The one real point in SimpleRPG's favor (it's already turn-based + player-input-driven) is being addressed differently: see the turn-based toggle below, which is additive rather than a full conversion.

### The structural idea: Dungeon = Trial, Overworld = the persistent sandbox

- **Origin story**: the player character is a newly-sentient being who comes into existence *inside* the dungeon — no family, no prior world-knowledge, full autonomy and sentience from the start. This diegetically explains the blank-slate start and justifies the dungeon as a bounded, separate space rather than a gameplay-convenience hand-wave.
- **The Dungeon** is close to what already exists — procedural maze floors, the auto-play combat loop, `EnemyFactory`-generated enemies, loot, XP, the Codex. Analogous to a **Trial** in J.T. Wright's *The Infinite World* web serial (the same series referenced in `Ideas/infinite_world_system_reference_manual.html` found during the SimpleRPG review) — a bounded high-risk/high-reward space you enter and return from, rather than the entire game.
- **Safe room + Guardian gate, every 4 floors — IMPLEMENTED 2026-07-24** (see progress log below for the full build). Corrected model vs. the first draft of this section: the safe room is an **interstitial space** between floor groups (e.g. "floor 4.5"), not a floor that replaces floor 5 — floors 1-4 are normal, then a safe room, then floors 5-8 resume normally, then another safe room, etc. Regular floors no longer have their own embedded boss at all (removed) — the only boss-tier fight is the gate **Guardian**, met by choice in the safe room. A bespoke small room with a shrine glowing faint blue (mana-colored) in the center, and a door on the far side leading to that gate's Guardian. Touch the shrine to return to the Overworld (currently a placeholder hook, since the Overworld doesn't exist yet); push through the far door to challenge the Guardian. Restful effect while inside (boosted HP/Stamina/Mana/Faith regen). Deeper gates' Guardians scale up naturally (same level-scaling formula as the old per-floor boss, fed a higher `CurrentFloor`). Leaving is never gated behind beating a Guardian.
- **The Overworld** is new: a persistent, open, capability-driven sandbox in the Cataclysm/DF spirit. This is where the "endless choices" vision actually lives — mining, crafting, building/dismantling structures, running a merchant stall, magical research — and where ChatGPT's proposed architecture (Simulation / World / Actors / Actions / Activities / Content registry / thin UI / Persistence, with Cataclysm-style data-driven `copy-from` inheritance and a start/do_turn/finish/cancel/resume Activities model) is the target internal shape. The spell-as-grammar idea (Effect + Element + Delivery + Shape + Duration + Modifiers + Cost) for *inventing new base spells* is complementary to, not competing with, our existing `CombinationEngine` (which handles *recombining existing* spells/items/abilities) — both are likely wanted eventually.
- **Pause vs. turn-based toggle (finalized 2026-07-24)**: two distinct, additive features. (1) A plain **pause**, available at any time, anywhere — freezes the tick driver, no stepping. (2) A **turn-based mode**, intended mainly for complex/difficult moments (a hard fight, a delicate crafting/building action) — the tick driver pauses and advances deliberately on player input instead of the timer. Real-time auto-play stays the default everywhere; neither feature is a control-model conversion.
- **First-region scope (finalized 2026-07-24)**: see [Starting Region.md](Starting%20Region.md) for the full spec — a single procedurally-generated-once-then-fixed town anchored at the dungeon entrance, small enough to build and verify end-to-end before expanding.

**Still open**: whether the dungeon (beyond the first exit opportunity) is a true one-time prologue or an ongoing loop the player can return to later — deferred; not load-bearing for the first slice, since the first slice only needs one clean entry→exit pass to prove the Dungeon→Overworld handoff.

**2026-07-24 — Overworld vertical slice IMPLEMENTED and verified.** Full plan executed in 7 build-and-verify steps (plan saved at the time to `purring-enchanting-cocoa.md`; informed by an Explore-agent fact-check and a Plan-agent critique of the draft design). Build clean throughout; full regression suite intact (`TEST_SIM` 17/17, `TEST_DUNGEON` all 4 assertions, `TEST_COMBINE`/`TEST_LOOT`/`TEST_BALANCE`/`TEST_CODEX` unchanged); new `TEST_OVERWORLD=1` proves the whole chain end-to-end with one Hero object throughout: dungeon exit → walk to mine → mine ore → walk to smithy → smelt + craft a sword → walk to stall → sell it (Gold 0→30, exact rarity-formula match) → walk to dungeon entrance → fresh dive, hero preserved (level/XP unchanged by the trip).

- **Step 1 — generalized the Activity pattern.** New `Core/Systems/Activity.cs` (abstract, `OnStart/OnTick/OnFinish/OnCancel(GameState)` — takes `GameState` not bare `Hero`, since almost every real activity needs floor/RNG/AcquireLoot context beyond the hero itself). Migrated the *existing* chest-opening mechanic onto `ChestOpenActivity` in the same pass (removed `Hero.IsOpeningChest`/`ChestOpeningTicks`/`ChestOpeningDuration` entirely) so there's one "is the hero busy" signal (`GameState.CurrentActivity`), not two that could desync. Verified bit-for-bit identical chest timing (91 ticks = the same 90-tick/3s duration + 1 trigger tick) before building anything new on the abstraction.
- **Placement note**: `Activity` and its concrete subclasses live in `Core/Systems/`, not `Core/Models/` — confirmed via grep that no existing Model references a Service, and Activities inherently mutate `GameState` (behavior, not data), so `Core/Systems` (where `CombatSystem`/`MovementSystem`/`MazeGenerator` already live) is the consistent home.
- **Step 2 — Overworld map + entry/exit.** New `MazeFeatureType` values (`DungeonEntrance`, `MineEntrance`, `Smithy`, `Stall`) and `Core/Systems/OverworldGenerator.cs` — a small (31×15), hand-authored, fixed layout (not runtime-procedural), matching `Starting Region.md`'s "generate once, then permanent" approach. `GameState.IsInOverworld` mirrors `IsInSafeRoom`'s mode-flag pattern; `ExitDungeonToOverworld` split into `EnterOverworld()` (the shrine's real hand-off — no longer a placeholder) and `StartFreshDungeonDive()` (the return trip, triggered by reaching `DungeonEntrance` while in the Overworld). **Real bug caught by the first test run**: the hero was placed *exactly on* the `DungeonEntrance` feature, which is also the return-trip trigger — so it bounced straight back into a new dive before ever getting a turn. Fixed by placing the hero one tile off the entrance (also just more realistic). `MazeRenderer` got explicit draw cases for all 4 new types (confirmed via the Plan-agent critique that the existing `switch` has no `default`, so skipping this would have silently rendered nothing).
- **Step 3 — `OverworldGoal` state machine.** The piece the critique flagged as actually missing from the first draft: nothing decided "walk to the mine, then the smithy, then..." `OverworldGoal` enum (`ToMine/Mining/ToSmithy/Crafting/ToStall/Selling/ToDungeonEntrance`) drives `Tick()`'s movement branch exactly like the safe room's Guardian-door default — an explicit, flagged **placeholder** scripted sequence standing in for real player choice, not "AI deciding." Verified via real auto-play ticks (no teleporting): distance-to-mine dropped from 4.00 to 0.43 over 60 ticks.
- **Step 4 — mining.** `Data/Materials/materials.json` + `Core/Services/MaterialDataService.cs` (a static singleton like `GameSettings`/`CodexService` — not an instance field like `CharacterDataService` — because materials are read-only content that needs to be reachable from a validation choke-point without threading an instance through). `Hero.Resources : Dictionary<string,int>` (plain data, no Service dependency) + `GameState.AddHeroResource` as the single validated write path. `MineOreActivity` — verified iron-ore 0→3, goal correctly advanced to `ToSmithy`.
- **Step 5 — crafting + Forge reconciliation.** `Data/Recipes/recipes.json` (smelt ore→ingot, craft ingot→sword) + `RecipeDataService` (same static-singleton pattern) + `CraftActivity` (generic — takes a caller-supplied `Action<GameState> onComplete` rather than hardcoding "what happens next," so the Smithy chains two recipes by nesting the continuation, and the activity itself stays reusable). Output either restocks a resource or produces a real `Weapon` via `CraftedItemCatalog` fed through the *existing* `AcquireLoot` pipeline — a crafted sword behaves identically to dungeon loot. **Caught mid-build**: the recipe numbers weren't internally consistent (mining 3 ore, smelting 2→1 ingot, but crafting needed 2 ingots — short by one with a single pass); fixed by dropping the craft recipe to 1 ingot. Also bumped `CombinationEngine.RarityPoints` from `private` to `internal` and added `GameState.CombineAtForge` — reconciling with, not duplicating, the `CombineLocation.Forge`-gated merge system already built in Phase 1 (verified reachable: `Sword + Dagger = Rapier`, the exact Phase 1 `RecipeBook` combo). Verified: sword crafted, ingot correctly consumed to 0.
- **Step 6 — selling.** `Hero.Gold`, `GameState.SellItem` reusing the now-`internal` `RarityPoints` formula (× a gold-per-point multiplier) rather than a second pricing formula. Verified: Gold 0→30 for the Common-rarity sword, sword removed from inventory/loadout.
- **Process note**: several `TEST_OVERWORLD` steps initially "failed" only because the demo's own tick budget was too small for the hero to actually walk the Overworld's real distances (e.g. mine→smithy ≈13 tiles) — not real bugs. Distinguishing "ran out of ticks" from "logic is wrong" mattered each time; increasing the budget and re-running confirmed the underlying mechanics were correct throughout.

**Explicitly deferred** (per the plan's stated scope): NPC/population simulation (the 100 residents, guards, water elementalists, sewer monsters), the training school, the hidden dark-temple tunnel, the fuller town geography beyond these 4 points of interest, a real player-input/decision system (the `OverworldGoal` sequence remains a flagged stand-in), and procedural town generation at runtime (this layout is hand-authored and fixed, per `Starting Region.md`). Real save-to-disk persistence — listed as deferred above — was picked up immediately after and is now done; see below.

**2026-07-24 — Real save/load persistence IMPLEMENTED and verified.** Closes the "only survives within one running process" gap flagged right above. Chosen as the next piece specifically because it's mechanically well-defined (extends the already-proven `CodexService` JSON pattern) and needed no real-time design input — picked deliberately while the owner was away from a computer to test.
- **Polymorphic serialization**: `Combinable` now carries `[JsonPolymorphic]`/`[JsonDerivedType]` attributes (`Weapon`/`Spell`/`Ability`/`Item`, discriminated by a `$kind` field) so `List<Combinable>` (Loadout/Inventory) round-trips through JSON without losing which concrete type — and therefore which fields (`BaseDamage`, `ManaCost`, `Modifiers`, ...) — each entry actually is.
- **`Core/Models/SaveData.cs`** — a flat snapshot (hero identity, level/XP, base stats, HP, Gold, Resources, Loadout, Inventory). Deliberately **Overworld-scoped only**: the dungeon is a transient per-dive space (the "Trial"), so a save always resumes in the Overworld, never mid-dive — no maze/enemy/combat state needs to round-trip.
- **`Core/Services/SaveService.cs`** — a plain static utility (`Save`/`Load`/`SaveExists`), not a singleton like `CodexService`/`GameSettings`, since there's no ongoing in-memory state to track between discrete save/load actions. Writes `Saves/save.json` (same gitignored folder as the Codex).
- **`GameState.LoadFrom(SaveData)`** — call after constructing a `GameState` normally with the saved class/race (so class-derived setup — effectiveness, starting loadout — computes correctly), then this overwrites the fresh stats/inventory with the saved ones and redirects into the Overworld.
- **Auto-save checkpoint**: `EnterOverworld()` now calls `SaveService.Save(this)` — there's no player-driven "Save" action yet (no input system at all exists), so entering the Overworld (having just survived leaving the dungeon) is the natural automatic checkpoint until one does.
- **Verified** via new `TEST_SAVE=1`: built up real progress (level 3, Gold 42, 5 iron-ore, a crafted Iron Sword) in one `GameState`, confirmed the shrine's auto-save wrote the file, then constructed a **completely separate** `GameState` instance and loaded into it — exact match on every field, including the polymorphic sword's `BaseDamage: 7` surviving the round-trip. Inspected the actual JSON: clean, human-readable, correct `$kind` discriminators.
- **Open decision — resolved 2026-07-24 by the owner**: "a Continue button that leads to a saves window showing name/class/level/playtime; select one and click Load." See the entry immediately below for the build.

**2026-07-24 — Continue button + multi-slot saves IMPLEMENTED and verified.** Resolves the open UI-integration question above with the owner's exact spec.
- **Multi-slot rework**: a single overwritten `Saves/save.json` doesn't support "pick a character from a list," so saves moved to one file per character: `Saves/Characters/{SaveId}.json`. `GameState.SaveId` (a GUID) identifies a character's slot — freshly generated for a new character, restored from the save on load — so re-saving overwrites the same slot instead of multiplying files. `SaveData`/`SaveSummary` gained `SaveId`, `PlaytimeSeconds`, `SavedAtUtc`. `GameState.TotalPlaytimeSeconds` = prior saved playtime + this session's elapsed ticks, so playtime accumulates correctly across app restarts.
- **`SaveService.ListSaves()`** — enumerates all slots, newest-first, for the picker. **Real bug caught during verification**: it originally globbed `Saves/*.json`, but `CodexService` also writes `Saves/codex.json` there — that file was getting misparsed as a phantom "Level 0" save. Fixed by giving character saves their own `Saves/Characters/` subdirectory rather than sharing the Codex's folder.
- **UI**: `CharacterSelectWindow` gained a Continue button (enabled only when `SaveService.HasAnySaves()`), opening a new `SavesWindow` that lists each slot as "Name — Race Class, Level N (playtime played)". Picking one and clicking Load flows the chosen `SaveId` back through `CharacterSelectWindow.LoadedSaveId` → `App.axaml.cs` → a new `MainWindowViewModel(SaveData)` constructor (parallel to the existing name/class/race constructor) that builds the `GameState` and calls `LoadFrom` instead of starting fresh.
- **Verified**: full regression suite unchanged; extended `TEST_SAVE=1` to also construct `MainWindowViewModel(SaveData)` directly (the exact code path Continue drives) and confirmed it lands the loaded hero back in the Overworld with the tick loop running. Launched the actual GUI briefly to confirm no XAML/startup errors. **Not verified**: an actual click-through of Continue → Saves window → Load in the running app — no GUI automation available in this environment, so the window layout/button feel still wants a real eyeball pass.

**2026-07-24 — Saves-window polish, done autonomously while the owner was away (small, no design judgment needed).**
- **`SaveService.Delete(saveId)`** + a Delete button in `SavesWindow`. Two-click confirm (click once arms it, button relabels to "Confirm Delete?", a second click on the same item commits) rather than a modal dialog — this codebase has no message-box abstraction, and that's cheap enough not to need one. Selecting a different save resets the armed state. `CharacterSelectWindow`'s Continue button re-checks `HasAnySaves()` after the saves window closes, in case deletions emptied the list.
- **Hardened the Continue load-failure edge case**: previously, if `SaveService.Load(saveId)` somehow returned null after the player explicitly picked a save (e.g. the file got deleted on disk between listing and loading), the app silently fell through to a brand-new default "Hero" character — misleading, since it looks like continuing but isn't. `App.axaml.cs` restructured so `ShowCharacterSelect` is re-entrant: on that failure it just loops back to character selection instead of substituting a fake character.
- **Verified**: rebuilt clean; extended `TEST_SAVE=1` to call `SaveService.Delete` and confirmed `HasAnySaves()`/`Load()` both correctly reflect the deletion; full regression suite re-run, unchanged; GUI launched briefly, no startup errors. Same caveat as above — not click-tested visually.

**2026-07-24 — Session code review (owner-requested) + fixes.** Full pass over every file changed this session. Verdict: structurally sound; 5 real findings, all fixed and re-verified: (1) `MineOreActivity` hardcoded the scripted goal advance — now takes a continuation like `CraftActivity`; (2) `SellItem` paid gold for unowned items — now ownership-guarded; (3) `CraftActivity` could drive resources negative — now validates inputs before consuming; (4) stale duplicated doc comment on `EnterOverworld` removed; (5) `StatsOverlay`'s hardcoded `9*7` species count now derived from `CharacterDataService`. Left alone deliberately: the flagged vertical-slice script ids in `CheckFeatures`, and the pre-existing regen-accumulator triplication in `Tick()`.

**2026-07-24 — Owner design rulings (recorded):**
- **Gate cadence corrected**: Guardian floors are every **5th** floor (5, 10, 15, ...), safe room just before each (4.5, 9.5, 14.5, ...) — not every 4th. The Guardian fight itself IS the Guardian floor: approaching the door advances `CurrentFloor` (so a 4.5 door fight is floor 5, scaling off 5), and victory proceeds to floor 6. Verified in `TEST_DUNGEON` through the second gate (9.5 → Guardian at 10).
- **Save rules**: no mid-dungeon saving, except safe rooms (auto-checkpoint on entry; continuing resumes *back in that safe room*). Closing mid-dungeon reverts to the dungeon entrance with entry-time state (auto-snapshot taken in `StartFreshDungeonDive`, recorded while still in the Overworld). **Death deletes the hero's save entirely (permadeath)** — wired into the death tick. `GameState.CanSave` (Overworld, or safe room with no Guardian engaged) is the single source of truth the pause menu uses.
- **Creation-time save**: starting a new game immediately writes a `ResumePoint.DungeonStart` save (done in the ViewModel path, not the GameState constructor, so headless tests don't litter slots) — a crash before the first safe room preserves the *character* (skip re-creation) while the run itself restarts from floor 1. `SaveData.ResumePoint` enum (string-serialized): `DungeonStart` / `OverworldEntrance` / `SafeRoom` (+ `SafeRoomFloor`).
- All of the above verified headlessly in the extended `TEST_SAVE`/`TEST_DUNGEON`.

**2026-07-24 — Title screen + ESC pause menu (owner spec) + visual direction.** Owner feedback: SimpleRPG's main screen/vibe is the reference; TheMazeRPG "feels lifeless." Direction adopted: SimpleRPG's dark terminal-roguelike aesthetic — near-black palette, the Odderf Basic pixel font (copied from `D:\Code\SimpleRPG\assets\fonts` into `Assets/Fonts`), glowing title, dark buttons with colored borders, ASCII flourish.
- **`Views/TitleWindow`** (new): black home screen, glowing gold "THE MAZE RPG", subtitle, ASCII flourish, pulsing "[ Press any key to start ]" → menu with **Start a New Game** / **Continue** (enabled only when saves exist; opens the existing SavesWindow).
- **ESC pause menu** in `MainWindow`: pauses the sim (`IsRunning = false`) while open. Resume / Save (enabled per `CanSave`, with a hint line explaining why when disabled) / Settings (disabled placeholder) / Quit to Main Menu / Quit to Desktop. Both quits auto-save when saving is allowed; mid-dungeon they intentionally don't (the entry snapshot is the revert point).
- **App flow rewired** (`App.axaml.cs`): Title → (New Game → CharacterSelect | Continue → SavesWindow) → game; backing out of creation returns to the title; `App.ReturnToTitle` handles in-game quit-to-menu. `CharacterSelectWindow` simplified back to pure creation (its interim Continue button moved to the title menu).
- **Verified**: full 8-suite regression clean; GUI launches with no XAML errors. **Not visually verified** (no GUI automation): font rendering, title glow/pulse, pause-menu layout — needs the owner's eyeball. Deeper "life" work explicitly still open: in-game message log (SimpleRPG-style, feeding off existing GameLog call sites), fog-of-war darkness in the dungeon renderer, dark-theming the in-game HUD/stats to match.

**2026-07-24 — Single-window shell refactor (owner correction).** The first title-screen build navigated by closing one window and opening the next; the owner's rule: **the app must never close its window to open another** — content switches in place. Restructured to the standard shell pattern:
- `Views/MainWindow` is now the one persistent window: a `ContentControl` host (900×600, black, Odderf font set window-wide) that swaps between three new `UserControl`s — `TitleView`, `CharacterSelectView` (gained a Back button since there's no window to close), and `GameView` (canvas + HUD overlays + ESC pause menu, extracted from the old MainWindow).
- Views raise events (`NewGameRequested`, `Confirmed(name,class,race)`, `QuitToMenuRequested`, ...) and the shell decides what to show — views never touch windows. The shell owns window-level `KeyDown` and routes to the active view (title's any-key, game's Tab/ESC). `SavesWindow` stays the one modal dialog (a child popup, not a window swap).
- `TitleWindow`/`CharacterSelectWindow` deleted; `App.axaml.cs` reduced to `desktop.MainWindow = new MainWindow()`. Quit-to-desktop = closing the only window; quit-to-menu stops the sim loop (`_activeViewModel.Stop()`) before swapping back to a fresh `TitleView`.
- Verified: full 8-suite regression clean, GUI launches with no XAML errors. Same standing caveat: layout/feel needs the owner's eyes.

**2026-07-24 — In-game visual overhaul: SimpleRPG's dark look (owner request: "the tile appearance is way better in the other game, and it has the menu screens that look great").** Studied `D:\Code\SimpleRPG`'s `display.js`/`constants.js` directly and ported its visual language:
- **Tiles** (`MazeRenderer`): pure-black void, floors `#222` with a `#333` center dot, walls `#666` with an `#888` inner stroke — SimpleRPG's exact palette/detailing.
- **Fog of war (render-only)**: never-seen = pure black; seen-but-out-of-sight = 30% alpha; currently visible (sight radius + real LOS from `GameState.CheckLOS`) = full. The renderer keeps its own per-maze seen-tiles memory so gameplay's `Maze.Explored` (the walked trail that drives auto-explore) is untouched — zero gameplay change, verified by the regression suite. Walls light up from their adjacent floors (walls block LOS so they're never directly "visible"). Overworld/safe rooms are exempt (fully lit spaces). Features on remembered tiles render dimmed; **enemies only render while actually visible** (range + LOS), SimpleRPG's rule.
- **HUD**: SimpleRPG stat-bar chrome — dark tinted backs (`#300`/`#030`/`#006`/`#330`), vertical-gradient fills, thin gray borders, centered shadowed text, plus a gold identity line ("Race Class Lv:N") and a gold bottom info line (location/Atk/Def/Gold). All Skia text now uses the Odderf pixel font (loaded once from the Avalonia asset, monospace fallback).
- **Menu panels**: `StatsOverlay` now draws SimpleRPG panel chrome (near-opaque black backdrop, `#111` panel, gold double border, gold headers, gray text, game font, matching tooltip). `DeathOverlay` rebuilt as SimpleRPG's death popup (`#111` panel, red `#600`/`#300` double border, "YOU DIED" in red, run summary lines, dark gold-bordered buttons).
- Verified: build clean, regression suites pass (fog is render-only), GUI launches. Needs the owner's eyeball for the actual look.

**2026-07-24 — Debug race, message log, and title Codex screens.**
- **Debug testing race** (`Data/Races/races.json` "Debug", `#FF00FF`): flat 1000 HP/Stamina/Mana via new optional `CharacterRace.HealthOverride/StaminaOverride/ManaOverride` fields, applied last in `UpdateHeroResourcePools` so they win over stat-derived pools. Marked `debug: true`; `EnemyFactory.PickRace` and `StatsOverlay`'s bestiary-total both skip debug races, so it never spawns as an enemy and doesn't inflate the discovered/total count. Selectable in character creation. Verified via `TEST_DEBUGRACE` (1000 pools confirmed; 0 debug enemies in 500 spawns).
- **Message log** (`Core/Services/MessageLog.cs`): a per-`GameState` 50-entry ring buffer of player-facing `GameMessage`s (text + `MessageKind` + tick), distinct from `GameLog` (dev/debug console). `GameState.LogMessage` called at all the meaningful beats — floor descents, safe-room entry, guardian appear/defeat, kills (+level-ups), chest/mine/craft/sell loot, traps, death, town arrival. Cleared on `RestartGame`. Rendered in `MazeRenderer.DrawMessageLog` bottom-left above the floor line: last 6 lines, newest at bottom, older lines fading toward the top, colored by kind (System=gray, Combat=red, Loot=gold, LevelUp=green, Warning=orange), Odderf font with shadow. Render-only — verified via `TEST_DUNGEON` (16 entries after a real multi-floor run).
- **Title Codex screens** (`Views/CodexWindow.axaml[.cs]`, `CodexTab` enum): dark gold-bordered modal (same popover pattern as SavesWindow) showing either the **Bestiary** (discovered "{Race} {Class}" archetypes, seen/killed/floor range, ordered by first-seen floor) or **Play Stats** (kills, deaths, deepest floor, floors cleared, dungeon exits, creatures discovered), both fed from `CodexService`. Two new title-menu buttons with **earned visibility** (SimpleRPG-style): Bestiary appears only once something's been encountered, Play Stats only once any cross-run activity exists.
- Verified: full 9-demo regression suite clean, GUI launches without errors. Needs the owner's eyeball for the actual look/placement of the message log and Codex panels.

**2026-07-25 — Player controls as a mode (the agency layer, first cut).** The long-flagged "no player-input system" gap, done the way the owner framed it: *modes*, never removing auto-run. `GameState.ControlMode` (`Auto` default / `Manual`), toggled live.
- **Auto** is the unchanged screensaver behavior (auto-explore, goal-walk, combat approach).
- **Manual** hands movement to the player: `Tick()`'s movement block branches to `MovementSystem.MoveHeroByDirection` (free directional movement, vector-normalized so diagonals aren't faster, per-axis round-to-cell walkability so the hero wall-slides), driven by a `_manualMoveX/Y` intent the UI sets from held keys. Auto-explore/goal/approach are all skipped; **attacks still auto-fire** (CheckCombat is untouched), so the player positions and the hero swings — no aiming needed for v1.
- **Input** (`GameView` + shell KeyUp routing): WASD/arrows move; pressing a move key from Auto drops straight into Manual; `M` explicitly toggles. Held-key set → intent vector, recomputed on every down/up. Pausing clears held keys + zeroes intent so the hero doesn't drift on un-pause. HUD shows an AUTO(green)/MANUAL(gold) indicator bottom-right (clear of the top-right Stats button) with a hint.
- **Defaults**: a bare `GameState` defaults to **Auto** (correct for the headless auto-play demos), but the player-facing game starts in **Manual** — set at the live-game seam in `MainWindowViewModel.Initialize` (2026-07-25, owner request). Session-only; not saved.
- Verified via new `TEST_CONTROL` (auto moves unattended; manual idle holds; manual+right moves then wall-stops; toggle-back resumes auto) plus the full 10-demo regression suite; GUI launches clean. Needs the owner's eyeball for movement feel/speed (`baseSpeed 0.09` in `MoveHeroByDirection`).
- **Deferred follow-ups to the agency layer** (owner-requested 2026-07-25, not yet built): **manual attacks** (a swing/aim input so combat isn't only auto-fire-while-positioning), and **manual stat assignment** (spend level-up points by hand rather than the class-growth auto-allocation).

**2026-07-25 — Manual combat: click-to-fire, hotbar, and gear-assignment menu.** Completes the agency layer's combat side (the two deferred follow-ups from the control-modes entry) plus the requested hotbar/assignment UI.
- **Click-to-fire (directional)**: clicking aims the current attack toward the cursor. `GameCanvas.OnPointerPressed` maps the click to world coords via new `MazeRenderer.ScreenToWorld` (uses a stored shake-free camera offset so aim doesn't jitter) and calls `GameState.FireManualAttack(dx,dy)` (no-op in Auto / on cooldown / unaffordable / paused). `CombatSystem.PerformHeroDirectionalAttack` spawns a projectile along the aim; unlike target-locked auto shots it doesn't know its victim, so it carries pre-defense `Projectile.StatDamage` and the hit is resolved against whatever enemy it strikes (`GameState.ResolveStatDamage`, mirroring the auto path's defense/crit/variance math). Ranged/magic fly ~14 tiles; melee-ish are a short lunge hitbox.
- **Refactor (no behaviour change to auto-combat)**: extracted `ComputeHeroStatDamage` + a shared `SpawnHeroProjectile` (the per-animation visual/speed/lifetime/hitbox switch) out of `PerformHeroAttack`, reused by both the target-locked and directional paths — no duplicated projectile spec. Verified auto-combat unchanged (`TEST_SIM`/`TEST_BALANCE`/`TEST_CODEX` all still kill/deal damage as before).
- **Manual vs auto combat gating**: in Manual mode `CheckCombat` runs only the *enemies'* attacks (they still fight back) and the hero fires by click; the smart attack-rotation and target-locked auto-fire are Auto-only, so the player's hotbar pick sticks. Hero attack cooldown + lunge-offset decay tick in the Manual branch of `Tick()`.
- **Hotbar** (`MazeRenderer.DrawHotbar`, bottom-center): one slot per `Hero.Attacks` entry, numbered 1–9, current highlighted gold, heavy-attack costs shown. Number keys (top row + numpad) select via `GameState.SelectAttack`.
- **Assignment menu** (`GameView` inventory overlay, `I` key): two columns — Equipped (hotbar) and Inventory — click to equip/unequip attack-gear between `Hero.Loadout` and `Hero.Inventory` (`GameState.EquipFromInventory`/`UnequipToInventory`, respecting `HotbarCapacity`), rarity-colored. Pauses the sim while open; Escape backs out of it before the pause menu. Fixed `StatsOverlay` to start non-hit-test-visible so clicks reach the canvas.
- Verified via extended `TEST_CONTROL` (directional shot drops an enemy 88→69; number-key select switches Quick Slash→Heavy Cleave) + full 10-demo regression + GUI smoke. **Needs the owner's hands-on feel**: aim/fire responsiveness, hotbar readability, whether melee click-range feels right (travel = `max(attack.Range,1)`), and enemy click-through (clicking on-screen UI vs. world).

**2026-07-25 — Hotbar polish: scroll-wheel switching + per-element spell colors.**
- **Scroll wheel** cycles the selected hotbar attack (`GameCanvas.OnPointerWheel` → `GameState.CycleAttack(±1)`, wrapping). Complements the number keys.
- **Magic element colors**: new `MagicElement` enum + `MagicElements.For(attack)` (id-authoritative with a cosmetic name-keyword fallback for future/combined spells), carried on `Projectile.Element` (set in both hero and enemy spawns). The renderer's magic projectile visuals (MagicComet/MagicMissile/ArcaneRing/Sonic) now tint from an element palette — Fire reddish-orange, Ice pale cyan, Poison neon green, Water blue, Lightning yellow, Holy gold, Mana pale blue, Arcane violet, Death/Shadow dark purple (lightened so they read on black), Earth brown, Air pale, Sonic light blue — falling back to each visual's original color for `MagicElement.None` (non-elemental attacks unchanged). Current mappings: fireball→Fire, ice-shard→Ice, magic-dart→Arcane, mana-bolt→Mana, holy-touch/divine-wrath→Holy, sound-wave/sonic-boom→Sonic.
- Verified via extended `TEST_CONTROL` (scroll wraps slot 0→last; element mapping table correct) + full 10-demo regression + GUI smoke.

**2026-07-25 — Elemental affinity system (foundation of the magic system).** First implementation of `Magic_System_Reference.md` §12/§16, planned in Plan Mode. Per-character, per-element rating (0–100, neutral 20) that drives four effects and grows with use. **Owner decisions:** grows-with-use; players **and** enemies have affinities; seeded from **race + class**.
- **Model/service**: `Core/Models/Affinities.cs` (dict keyed on `MagicElement`, `Get` returns neutral for unset) on both `Hero` and `Enemy`. `Core/Services/AffinityService.cs` holds the curves (all **identity at neutral 20**, anchored to §12's provisional table): `PowerMultiplier` (0.75→1.0→1.25), `ManaCostMultiplier` (1.60→1.0→0.80), `Resistance` (−0.30→0→+0.40), `LearnableTier`/`CanLearn` (affinity-gated tiers), an `ElementRelationships` graph (opposed/harmonious from §12), and `OnElementCast` growth (raise the cast element, spill into harmonious, drain opposed — the specialization tradeoff). **Opposed-drain is gated behind `ElementalistThreshold` (45)** (owner call 2026-07-25): casual/dabbling magic use never lowers your affinity to other elements — only a *committed elementalist* (affinity ≥ threshold in that element) erodes its opposites through further practice.
- **Element enum**: added `Life`, `Light`, `Void` to `MagicElement` (+ their §15 palette colors). `Holy` kept as a legacy divine tint (non-affinity) — full `MagicSource` rename + Holy→Light reconciliation deferred.
- **Combat integration**: caster power in `ComputeHeroStatDamage` (hero) and `PerformEnemyAttack` (enemy); target resistance in `PerformHeroAttack` (target-locked), `GameState.ResolveStatDamage` (directional shots, via `Projectile.Element`), and baked into enemy shots vs the hero; affinity-discounted mana in `DeductAttackCost` + every affordability gate (`FireManualAttack`, auto `canAfford`); growth on every hero cast (auto + manual). Element comes from the existing `MagicElements.For`.
- **Seeding**: optional `"affinities"` maps on classes/races (`Mage Apprentice`→Arcane/Fire/Mana, `Priest`→Light/Life, `Bard`→Sonic/Air; Elf/Dwarf/Dragonborn/Tiefling small racial leans), loaded like the existing stat maps and applied over baseline for hero (`CharacterDataService`) and enemies (`EnemyFactory`). Kept modest so balance shifts are small and caster-only.
- **UI**: an AFFINITIES section in the Tab stats overlay (non-neutral elements, element-colored, with learnable tier).
- **Verified** via new `TEST_AFFINITY` (seeding, power, resistance 100→65, mana cost 8/10/16, fire-cast growth Fire↑/Water·Ice↓, tier gate) + full 11-demo regression (neutral invariant holds — physical/Warrior paths unchanged; `TEST_BALANCE`'s printed estimate is its own pre-combat formula and doesn't route through the affinity path) + GUI smoke. Caught and fixed a sign error in the mana-cost curve during verification (unattuned was coming out cheaper, not costlier).
- **Deferred** (noted): full `MagicSource` rename + Holy→Light; wiring `CanLearn` into a real spell-research/learning flow; elemental status effects; enemy affinity growth; showing affinity-adjusted cost on the hotbar. Numbers are the doc's provisional values (§17 flags them as open) — tunable in `AffinityService`.

**2026-07-26 — Combat feel fixes (owner playtest feedback).** Three first-impressions from the owner's first play:
- **"Mana Dart is purple."** The Mage's basic bolt (id `magic-dart`, was named "Magic Dart") was mapped to Arcane (violet). Renamed to **"Mana Dart"**, remapped to `MagicElement.Mana` (pale blue), attribute Magic — the reliable ordered-mana bolt per the magic-system doc. `arcane-blast` stays Arcane. (Id unchanged, so visual/element lookups by id are unaffected.)
- **"Enemies don't follow when line of sight is lost."** They gave up instantly: the pursuit fallback in `CheckCombat` required LOS even for recent pursuers. Now an enemy within the agro radius with pursuit time left stays engaged **without** LOS, and the enemy-movement branch chases the hero's **last-known position** (new `_enemyLastKnownHeroPos`, recorded whenever an enemy has LOS) until the pursuit window (~3s) expires — so they round corners after you instead of stopping dead. Pursuit state cleared on `StartNewFloor`.
- **"Impossible to dodge — defeats the purpose of accuracy and moving."** No dodge/accuracy roll existed. Added `GameState.RollDodge(targetAgility, attackerAccuracy)` (base 3%, ±2%/point of Agility−accuracy, capped 50%) applied on every projectile contact in `ProcessProjectileCollisions` (both hero→enemy and enemy→hero); the attacker's accuracy rides on the projectile as `Projectile.Accuracy` (= effective Dexterity at fire time). A dodge consumes the shot, deals no damage, and logs "You dodge the attack!" / "The {race} {class} dodges!" — so Agility (dodge) and Dexterity (accuracy) now matter, on top of physically moving out of a projectile's fire-time path.
- Verified: `TEST_CONTROL` extended (a 40-Agility hero dodged 35 hits over 800 ticks vs a low-accuracy attacker); full 11-demo regression clean (dodge RNG doesn't break the combat demos — they print state, not exact HP); GUI smoke clean. **Not visually verified**: the pursuit chase and the dodge feel in the running game — needs the owner's eyes. Numbers (dodge base/scale/cap, pursuit window) are tunable in one spot each. A floating "Dodge" text over the character (vs. just the log line) is a noted nice-to-have.

**2026-07-26 — Active dodge, right-click interaction, trap perception, corpse looting (owner playtest asks; Plan Mode).** Four linked additions making positioning and awareness matter.
- **Active dodge (Space)**: a fast dash in the WASD direction (`GameState.TryDash` → `MovementSystem.MoveHeroByDirection` with a dash speed override) with **i-frames** (`IsHeroInvulnerable` negates the hit in `ProcessProjectileCollisions`) and a ~1s cooldown, shown as a HUD readiness bar. Dashes toward the last-faced direction when standing still.
- **Trap perception**: traps spawn `Hidden` and render at ~11% alpha (reusing `DrawFeatures`' `SaveLayer` wrapper) until noticed. `GameState.UpdatePerception` rolls a **Wisdom**-based `PerceptionService.SpotChancePerTick` each tick within 4 tiles + LOS; a spot flips `Perceived`, snaps the trap opaque, pops a **!** over the hero (`_heroAlertTicks`), and logs it. Unspotted traps still spring on contact.
- **Right-click interaction**: `GameCanvas` right-click hit-tests the nearest trap or corpse and raises `InteractRequested`; `GameView` shows a context menu at the point. Trap → *Examine* (reveals) / *Disarm* (perceived only). Disarm is a **Dexterity roll** (`PerceptionService.DisarmChance`); a failure may spring it.
- **Corpse looting**: kills now drop into `Enemy.Inventory` + a small `Gold` (not auto-collected). Right-click a corpse → *Examine* / *Loot*; *Loot* opens a two-panel window (body / player inventory) with double-click transfer, the ◂/▸ arrow control, and *Loot All* (`GameState.LootItem`/`LootAll`/`DepositToCorpse`). **Auto mode auto-loots nearby corpses** (`AutoLootNearbyCorpses`) so auto-play still collects loot. Chests unchanged. Enemies' actual equipped gear as loot is deferred (they have projected attacks, not Combinable loadouts).
- **Extensibility**: `PerceptionService` spot/disarm chances are `base + statTerm + abilityBonus`, where the `abilityBonus` hooks are where the owner's planned **Detect Traps**/**Disarm Traps** abilities + proficiencies will attach.
- Verified: new `TEST_INTERACT` (spot chance scales with Wisdom & drops to 0 past range; Examine reveals; Dex disarm 174 clean/13 sprung of 200; dash i-frames negate a 50-dmg shot; loot transfers items + 12 gold). Full 12-demo regression clean — one demo (`TEST_DUNGEON`) needed an enemy-clear so combat wouldn't block its scripted stairs-teleport on crowded deep floors (a demo-robustness fix surfaced by the earlier enemy-dodge change, not a product bug). GUI smoke clean. **Not visually verified**: context menu placement, trap transparency/**!**/dash-bar feel — needs the owner's hands-on pass. Tunables (dash timing, perception radius/base, disarm base) each in one spot.

**2026-07-26 — Playtest polish: arrows, wall-push, manual-only equip, item inspection (owner follow-up).** Four fixes on top of the batch above.
- **Arrow rendering**: dropped the growing shaft-line trail. A non-elemental arrow now draws as a short fixed arrow shape (12px shaft + fletching) using a stable heading from `StartX/Y → TargetX/Y`; only elemental arrows (`Element != None`) get a sparse 3-particle element-colored trail (Screen blend) behind them, in `MazeRenderer` (~line 1210).
- **Wall-push fix**: entering combat next to a wall no longer shoves the hero into it (losing their view). `ResolveHeroEnemyCollisions` now nudges per-axis via `TryNudgeHeroAxis` (moves only onto a walkable tile, returns success) and pushes the *enemy* out (`NudgeEnemyAxis`) when the hero can't yield without hitting a wall.
- **No auto-equip**: picking up loot never auto-equips. `AcquireLoot` just adds to `Hero.Inventory` and logs; the player equips spells/items themselves from the inventory screen.
- **Inventory/loot inspection + arrow-only transfer**: clicking an item row now shows its stats (damage/range/cooldown/crit/cost, attributes, level) in a details pane at the bottom of both the inventory and loot overlays (`ItemRow`/`ShowDetails`/`ItemStatsText`). Equip/unequip/loot-transfer happen **only** via the ◂/▸ arrow control (or double-click in the loot window) — a stray row click just inspects, never moves the item. Replaced the old `ItemButton` and removed the loot window's drag-and-drop path.
- Verified: clean build, full 12-demo regression clean (`TEST_LOOT` confirms pickups land in inventory "(not auto-equipped)" and manual `EquipFromInventory` works). GUI smoke not yet done by the owner — arrow look, wall-push behavior in a corner, and the click-to-inspect/arrow-to-transfer flow need the owner's eyes.

**2026-07-27 — Manual stat allocation (closes the deferred agency-layer follow-up).** Replaces class auto-growth on level-up with hand-assigned points. **Owner decisions:** *fully manual* (no class auto-allocation at all — classes now differ only by their starting stats), and points are *spent anytime from the stats screen* (they bank up; no forced level-up dialog).
- **Model** (`Hero`): `UnspentStatPoints` + `const StatPointsPerLevel = 5` (sized against the old class `StatGrowth` totals of ~4–7/level; concentrated manual spend is stronger per point, so 5 keeps power comparable). `LevelUp()` no longer reads `ClassData.StatGrowth` — it just grants the pool. Baseline per-level MaxHp/Attack/Defense gains and the level-5/10/15 attack unlocks are unchanged.
- **Spend path** (`GameState.SpendStatPoint(stat)`): validates the pool, increments the base stat, and refreshes derived caps via the existing `UpdateHeroResourcePools()`. Constitution also bumps `MaxHp` immediately (`HpPerConstitutionPoint = 5`, scaled by racial effectiveness) so the payoff is instant; every other stat's effect is already live through the `Effective*` values combat reads. Unknown stat names and an empty pool both no-op and return false.
- **UI** (`StatsOverlay`): the modal shows "N points to spend" by the STATS header and a clickable green **+** beside each stat while points remain. The overlay is custom-drawn, so it registers each **+** rect (in panel-local space, offset by the stored panel origin) and hit-tests them in a new `PointerPressed` handler — same pattern as the existing hover-tooltip areas.
- **Persistence**: `UnspentStatPoints` added to `SaveData`/`SaveService`/`GameState.LoadFrom` (defaults to 0 for older saves).
- Verified: new `TEST_STATS` (level-ups grant 5/level and *don't* touch core stats; spending raises the stat + MaxHp/Stamina/Mana caps and decrements the pool; unknown-stat and empty-pool both rejected; the pool round-trips through `LoadFrom`). Full 13-demo regression clean. **Not visually verified**: the **+** buttons' feel/placement in the running modal — needs the owner's eyes.

**2026-07-27 — In-game debug console (owner-requested cheat/testing tool).** A backtick (`` ` ``/`~`) key toggles a command bar at the bottom of the game view; type a command, Enter runs it, the result shows on the output line above the input. A leading `/` is optional.
- **Executor** (`GameState.ExecuteDebugCommand(raw)`): lives on `GameState` for full access to private floor/world state. Commands: `addxp N`, `addlevel N` (each level via the real `GainExperience` path, so it also grants stat points), `addpoints N`, `addgold N`, `additem <id> [n]`, `addspell <id> [n]`, `moveplayer dungeon N|overworld|safe N`, `reset health|mana|stamina|faith|all`, `listitems`, `help`. Every result is also mirrored to the message log.
- **Item/spell resolver**: `additem`/`addspell` resolve from a small registry sourced from `CombinableCatalog` + `CraftedItemCatalog` (fireball, ice-shard, bow, sword, dagger, shield-generator, dense-musculature, mana-circuitry, iron-sword), tolerant of case/spacing/hyphens with a contains-match fallback; unknown names return the available list. Added copies go to the **inventory** (consistent with the no-auto-equip rule).
- **UI/input** (`GameView`): a bottom `DebugConsoleOverlay` (output `TextBlock` + input `TextBox`). Opening pauses the sim and focuses the input; while open the console owns the keyboard — the shell's tunnel `HandleKey` runs the command on Enter, closes on Esc/backtick, and lets every other key fall through to the `TextBox` so typing works (movement keys are only marked Handled when the console is closed).
- Verified: new `TEST_CONSOLE` exercises every command (xp/level/points/gold, item+spell add with a tolerant match and an unknown-name error, reset, `moveplayer dungeon 4` → floor 4 then `overworld`, unknown-command handling). Full 14-demo regression clean. **Not verified**: the live keyboard capture (backtick toggle, typing while the game is paused) — needs the owner's hands-on check.

**2026-07-28 — Overworld input layer: player-driven town (Press-E), scripted sequence removed.** Closes the load-bearing agency gap flagged in §0a — the town is now player-driven instead of running a scripted `OverworldGoal` auto-walk. Planned in Plan Mode. **Owner decisions:** interaction = **proximity prompt + `E`**; **remove the scripted sequence entirely**; scope = **wire the 3 existing structures** + dungeon entrance (no new economy).
- **Removed the script**: deleted `OverworldGoal`, `CurrentOverworldGoal`, `OverworldGoalTarget`, `AdvanceOverworldGoal`, `OverworldTriggerAllowed`, the `IsInOverworld` auto-walk branch in `Tick()`, and the overworld auto-trigger blocks in `CheckFeatures()` (structures are no longer used by walking onto them). Dungeon-side trap/chest/shrine logic untouched.
- **Town is Manual + Press-E**: `EnterOverworld()`/overworld-resume set `ControlMode = Manual` (WASD movement). `GameState.NearbyInteractable` is recomputed each tick by `UpdateOverworldInteractable()` (nearest structure within `InteractRadius = 1.3` tiles, skipped mid-Activity). New player-action methods: `MineOre()`, `CanCraft(recipe)`/`Craft(recipe)` (validates `Hero.Resources`), `EnterDungeon()`; `SellPrice(item)` added and reused by `SellItem`.
- **UI** (`GameView`): a bottom-center `InteractPromptOverlay` ("Press [E] — Smithy") driven by a 30 Hz `DispatcherTimer` reading `NearbyInteractable` (stopped on view detach). `E` opens `OpenStructureMenu(feature)` — reuses the right-click context-menu overlay, opened **centered**, with per-type actions: Mine → "Mine Iron Ore"; Smithy → one entry per `RecipeDataService.Recipes`, disabled with "(need …)" when inputs are short; Stall → the new `SellOverlay`; DungeonEntrance → "Enter the Dungeon"; plus Cancel. The `SellOverlay` is a single-panel list reusing the `ItemRow`/`ShowDetails` pattern, selling via `SellItem` at `SellPrice`. Movement/dash now gate on a shared `AnyOverlayOpen`.
- **Deferred** (noted): Forge combine action (needs a two-item picker; `CombineAtForge` still exists and TEST_OVERWORLD proves it reachable), buy-back/full shop, multiple ores/recipes + a recipe "station" tag, NPCs, structure placement/building, right-click parity in town.
- Verified: `TEST_OVERWORLD` rewritten to drive the chain via the new methods (`ControlMode == Manual`; `MineOre` → ore 3; `Craft(smelt)` → ore 1/ingot 1; `Craft(sword)` → Iron Sword in inventory/ingot 0; `SellItem` → Gold +30; `EnterDungeon` → Floor 1, same Hero) — same end-state as the old script, minus the auto-walk. Full 14-demo regression clean. **Not visually verified**: WASD-to-structure, the `E` prompt/menu, and the sell window in the running game — needs the owner's hands-on pass.

**2026-07-29 — Sprite art: first pass (static frames) over the Pixel Crawler packs.** The owner dropped a large pixel-art library (the **Pixel Crawler** pack series) into `Assets/Sprites/`. Entities were procedural Skia shapes (hero = two circles, enemies = class-colored square/diamond/triangle/pentagon); they now draw real sprites where mapped. **Owner decisions:** *static frame first* (animation deferred), and *Claude proposes the id→sheet defaults, owner tweaks*.
- **What the packs actually are** (worth recording): 769 files, of which only 239 are usable PNGs — the rest is editor/source baggage (242 `.aseprite`, 239 `.import`, plus `.png~`/`.gif`/`.txt`). Character art is **horizontal animation strips** (`Idle`/`Run`/`Death`, some `Hit`), *not* single images, and the layout is fully derivable: **frame size = sheet height, frame count = width ÷ height** (e.g. Knight Idle 128×32 = four 32px frames). All RGBA. Heroes have no attack animation. Weapons/tilesets are 400×400 **atlases** needing slice-and-index mapping — deliberately not tackled yet.
- **Bundling fix (important)**: `AvaloniaResource Include="Assets\**"` was shipping the entire library — packs, `.aseprite` sources and all — inside the app. Now excludes `Assets\Sprites\Pixel Crawler*\**` and the junk extensions, so the bundle is **29 files** (font + icon + 27 curated sheets) instead of 769. The raw packs stay in the repo as an art library; to use a new sheet, copy it into a curated folder and map it.
- **Curated flat folders**: chosen idle sheets copied to `Assets/Sprites/characters/` (all 7 classes) and `Assets/Sprites/enemies/` (20 sheets — Skeleton crew as the class-level default, plus Orc / Elf / Human(Royal Crew) / Rat(→Kobold) race themes). Flat names avoid the packs' spaces-and-versions paths.
- **`Data/Sprites/sprites.json`** — a flat manifest with a documented lookup chain: hero = `hero:{Class}`; enemy = `enemy:{Race}:{Class}` → `enemy:{Race}` → `enemy:{Class}`. No wildcards, trivially extensible, and the class-level tier guarantees every race×class resolves.
- **`UI/Rendering/SpriteService.cs`** (in UI, not Core — it deals in SkiaSharp, and Core stays UI-agnostic so headless demos keep working): static singleton in the `MaterialDataService`/`RecipeDataService` mould; loads the manifest, decodes each sheet's **frame 0** by copying the leading N×N block, caches by path (**including null**, so a broken path is attempted once, not every frame). `CrispSize()` snaps the draw size to an **integer multiple of the frame size** (32px art → 2× in a 64px cell) and drawing uses `SKFilterQuality.None` — non-integer scaling with filtering is what makes pixel art look mushy.
- **Renderer wiring**: `DrawHero` and `DrawEnemies` draw the sprite when one resolves and otherwise **fall through to the original shapes**, so a partial/typo'd manifest degrades to the old look instead of crashing or drawing nothing. Enemy corpses tint dark via a `SrcATop` blend (matching the old dimmed-color treatment); elite/boss halo rings and health bars are unchanged. Note: the hero's **race** color is no longer conveyed (the sprite carries class identity only) — flagged for the animation/paperdoll pass.
- **Deferred**: animation (idle/run/death frames are already sliceable and present), attack poses (packs have none for heroes), weapon/tile **atlases** (need slice-index mapping), structures/items/tiles mapping, and race-distinguishing hero art.
- Verified: new `TEST_SPRITES` validates the manifest **against disk** (0 missing, 0 non-sliceable, 7/7 hero classes, 63/63 race×class resolving, plus printed specificity samples) **and exercises the real runtime path** — it calls `SetupWithoutStarting()` to init Avalonia headlessly and decodes through `SpriteService`'s `avares://` loader (12 sprites, 0 failures), which is the check that catches "file is fine on disk but silently renders as a circle". Full 15-demo regression clean; GUI launches without exceptions. **Not visually verified**: how the sprites actually look in play (scale/alignment against the 64px grid) — needs the owner's eyes.

**2026-08-01 — Retargeted to .NET 10 (+ Avalonia patch bump).** Straight framework upgrade, no code changes needed.
- **`net9.0` → `net10.0`** in `TheMazeRPG.csproj` — the only version reference in the repo (SDK 10.0.103 / runtime 10.0.3 were already installed). No source, API, or `LangVersion` changes were required; the project now compiles as C# 14 by TFM default.
- **Security finding the upgrade surfaced**: the build began emitting `NU1903` — *Tmds.DBus.Protocol 0.21.2 has a known high severity vulnerability* ([GHSA-xrw6-gwf8-vvr9](https://github.com/advisories/GHSA-xrw6-gwf8-vvr9), D-Bus signal spoofing / FD exhaustion). Not a regression: .NET 10's SDK defaults `NuGetAuditMode` to **all** (transitive) where .NET 9 audited **direct** packages only, so a pre-existing issue simply became visible. The package is transitive — `Avalonia.Desktop → Avalonia.X11 → Avalonia.FreeDesktop → Tmds.DBus.Protocol` — i.e. the **Linux/X11** backend, never loaded on Windows.
- **Fix**: advisory is patched in `0.21.3`; Avalonia 11.3.18 references exactly that, so all five Avalonia packages went **11.3.6 → 11.3.18** (patch-level, same minor line). Build is back to **0 warnings, 0 errors**.
- Verified: clean rebuild (`bin`/`obj` wiped so no stale `net9.0` artifacts), full **15-demo regression clean** both before and after the Avalonia bump, and the GUI launches with no exceptions. Note `TEST_SPRITES` exercises Avalonia's asset loader via `SetupWithoutStarting()`, so the upgrade's riskiest surface (resource loading) is covered headlessly.

This section supersedes/absorbs what's written as "Phase 2" further down (which assumed combining would be wired into the existing dungeon-only game) — that gets revisited once the Dungeon→Overworld handoff and first-region slice exist.

---

## 0. Progress log

**2026-07-23 — Phase 0 complete + first dream step.** Verified via clean build and a full headless class/race matrix (17/17 engagements resolve, no stalemates, max 20 ticks each).
- **0.1/0.2 Tick rate:** added `Core/Services/GameSettings.cs` (loads `settings.json`, authoritative `TickRate`, `SecondsToTicks`). Timer, all durations (pursuit/auto-restart/chest/attack-switch/combat-windup), and stamina/mana/faith/HP regen now derive from the tick rate. Death countdown shows real time.
- **0.3 Free first-strike:** fixed — combat entry is detected before the `InCombat` flag is set, so a ~0.3s wind-up now applies. Verified: Mage no longer one-shots at tick 0.
- **0.4 Dead kill block:** removed from `CombatSystem` (damage/XP is projectile-deferred in `GameState`).
- **0.5 Floor off-by-one:** `CurrentFloor` starts at 0; first floor is now Floor 1.
- **0.6 Debug spam:** added `Core/Services/GameLog.cs` (gated by `MAZE_VERBOSE=1`); per-tick combat + load diagnostics no longer spam the shipped app.
- **0.7 Dead code:** removed `GetAttackSpeedForType`, `Reset`, `MazeTest.cs`, `reservedCells`, the unused `time` var, and `Data/Classes/wanderer.json`.
- **0.8 Reflection:** `RestartGame` now assigns the reseeded systems directly (fields no longer `readonly`).
- **0.9 Stale logs:** deleted `playtrace.txt` / `playtrace_fixed.txt`.
- **Dream step 1 — XP curve:** `Hero.LevelUp` now uses the xlsx quadratic (`ExperienceToNext = 100 * Level^2`).
- **Bonus fix (surfaced by 0.3):** ranged/caster classes could stalemate — kiting backward pinned them at a sub-cell position whose `Floor` cell was a wall, permanently failing their own LOS check. Fixed in `MovementSystem.MoveHeroTowardEnemy`: with no LOS, close in to regain a clean shot instead of backing into a wall. See the root-cause note below.

**2026-07-23 (committed Phase 0) — coordinate root cause fixed.** The `Floor`/`Round` LOS inconsistency is resolved: `HasLineOfSight`, `GetSightLine`, and the vision cone in `GameState`, plus the LOS helpers in `CombatSystem` and `MovementSystem`, now all map a world position to its cell with `Round` (integer coords = cell centers), consistent with entity `GridX`, movement, and spawns. The vision cone was reworked to measure from the entity's actual position to integer cell centers. Verified: full class/race matrix still resolves 17/17 with no stalemates, so aggro/vision/combat are intact. (The earlier movement-layer symptom fix is kept as defensive behavior.)

**2026-07-23 — Phase 1 started: the `Combinable` keystone (§4).** Built the unified model layer and a working merge engine, self-contained and **not yet wired into live combat** (running game unaffected — sim still 17/17):
- `Core/Models/Combinable.cs` — base type + `Rarity`, `CombinableKind`, `GameAttribute` enums.
- `Core/Models/{Weapon,Spell,Ability,Item}.cs` — concrete kinds; `Weapon`/`Spell` project to the existing `Attack` via `ToAttack()` (the seam where AttackFactory becomes data-driven next).
- `Core/Services/CombinationEngine.cs` — implements the Game Idea.md rules: same-kind+same-attribute → intensify (rarity bumps, level resets); otherwise blend (attribute union + Fire/Shadow→Infernal, Fire/Ice→Frost, Air/Lightning→Storm synergies); result kind by carrier priority (Weapon>Armor>Item>Ability>Spell); rarity by point-band average; level = min, reset to 0 on rarity increase; location gating (spells anywhere / weapons+items at a Forge / abilities at a Shrine).
- `Core/Services/RecipeBook.cs` — hand-authored named combos from the doc (Frostfireball, Fire Bow, Bubble Bow, Fire Fist, Fire Affinity, Rapier); fall through to the generic engine otherwise.
- `Core/Models/CombinableCatalog.cs` — sample base combinables to bootstrap/demo (later moves to JSON under `Data/`).
- `Attack.Id` added (stable id foundation to retire the render/behavior substring-matching).
- Demo/verify path: `TEST_COMBINE=1` prints example combinations. Confirmed recipes, intensify, and generic cross-kind blends all produce correct kind/rarity/attributes.

**2026-07-23 — Phase 1: attacks are now data-driven from equipped gear.** Behavior-preserving (sim still 17/17; e.g. Warrior still opens with "Quick Slash" at range 1.2):
- `AttackFactory` rewritten: each class's starting attacks are `Weapon`/`Spell` instances (stable Ids, ported combat numbers, forward-looking attributes) projected to `Attack`s via `ToAttack()`. `GetStartingLoadout` returns the equippable `Combinable`s; `ToAttacks` projects them.
- `Hero.Loadout` (`List<Combinable>`) added — the hero now stores its equipped weapons/spells, and `Attacks` are projected from them (constructor + `RestartGame`). Foundation for inventory/equip and for merged gear changing your attacks.
- `Weapon.ToAttack`/`Spell.ToAttack` now carry `Id` (and `Spell` carries knockback). Every attack, including the level-up unlocks, has a stable `Id`.
- Resolved a real name collision the Ids expose: the level-15 unlock was also named "Arcane Blast" (different stats, no mana) and the renderer/collision substring-matching conflated it with the Mage's AoE spell — it accidentally rendered/behaved as an AoE ring. Renamed to **Arcane Bolt** (id `arcane-bolt`), so it's now a distinct single-target magic attack.

**2026-07-24 — Racial effectiveness system.** Implemented the owner's spec (see [Racial Effectiveness.md](Racial%20Effectiveness.md)). Verified: base stats display unchanged, effective values differ per race (Orc Warrior Str 8→11.2 ×1.4, Elf 8→6.4 ×0.8, Dwarf 8→10.4), resource pools derive from effective Con (Orc MaxStamina 204 vs Elf 172); build clean, sim 17/17.
- Roster is all **9 races** (Human, Elf, Dwarf, Halfling, Goblin, Kobold, Dragonborn, Orc, Tiefling) with the owner's finalized multipliers. `races.json` now stores per-attribute `effectiveness` multipliers (float), not additive ints.
- `CharacterRace.Effectiveness`, `Hero.*Effectiveness` + `Hero.Effective*` (float, unrounded). `ApplyClassAndRace` sets effectiveness and no longer touches base stats.
- All derived formulas use `Effective*`: combat damage/crit/cooldown (`CombatSystem`), stamina/mana/faith/HP regen + resource pools (`GameState`), movement speed from Agility (`MovementSystem`), and HP/attack/defense/XP gains (`Hero`). Requirements (base vs. labeled "effective") are noted for later — no requirement gates exist yet.
- `StatsOverlay` shows the base value as primary plus a color-coded inline `→ effective` and effectiveness % on hover.

**2026-07-24 — Projectile visuals/behavior migrated to stable ids.** Retired the last name-substring matching:
- New `VisualStyle` enum + `Projectile.Visual`; `AttackVisuals.For(attack)` maps by stable id with an animation-based fallback (so any future/merged attack still gets a sensible effect). The renderer's ~12 projectile branches now switch on `Visual`, not `AttackName`.
- Fixes verified via a `TEST_COMBINE` routing dump: **Archer's Quick Shot / Power Shot → Arrow** (were rendering as a plain line), **Bard's Sound Wave → Sonic**, **Mana Bolt → MagicComet** — all previously mis-routed because their names didn't match the old substrings.
- The Arcane Blast AoE behavior (expanding radius + multi-hit) now keys off `attack.Id == "arcane-blast"` / `Visual == ArcaneRing` instead of the name.

**Ready to eyeball in-app** (`dotnet run`): pick **Archer** to see arrows; pick a non-Human race (e.g. **Orc**/**Elf**) and press **Tab** to see base vs. effective stats. Report anything that looks off.

**2026-07-24 — Bugfix: lingering projectile artifact.** Projectile/hit-effect update+prune only ran at the end of `CheckCombat`, which returns early when no enemies are engaged — so the last attack's projectile froze in place after the killing blow and even carried into the next floor. Extracted the update/prune into `UpdateProjectilesAndEffects()`, now called every tick from `Tick()` (not just during combat), and `StartNewFloor` now clears `Projectiles`/`HitEffects`. Build clean, sim 17/17.

**2026-07-24 — Inventory / equip hotbar + chest loot.** Found gear now changes your kit. Verified via `TEST_LOOT=1` (Warrior picks up Bow then Sword → attacks grow to [Quick Slash, Heavy Cleave, Bow, Sword]; hotbar capacity 4 reached → further finds overflow to inventory). Build clean, sim 17/17.
- `Hero.Inventory` + `Hero.HotbarCapacity` (4). `LootService.Roll(floor, rng)` draws a weapon/spell/item from `CombinableCatalog` with floor-scaled rarity.
- Opening a chest now drops loot (in addition to the stairs key). `GameState.AcquireLoot` auto-equips attack-gear into a free hotbar slot and re-projects `Hero.Attacks` (`RefreshAttacks`, preserving the current attack); non-attack gear or overflow goes to inventory. Auto-equip stands in for the manual "swap/combine/send" choice while auto-played.
- `StatsOverlay` shows an INVENTORY section, rarity color-coded (Common→Mythic per the doc).
- *Tunable:* the rarity curve (`LootService.RollRarity`, `roll = Next(100) + floor*4`) is a first pass and a touch generous at low floors — easy to retune later.

**2026-07-24 — Enemy damage buff (balance).** Owner reported floor-1 enemies felt harmless (they did ~2–6/hit vs 100 HP because Strength/Dexterity are ~1 at low level and base attack was tiny). Buffed: regular `baseAtk 3+lvl → 6+lvl*2`, regular `Strength +2` / `Dexterity +1` base, boss `Attack 8+floor*2 → 12+floor*3`. Now floor-1 regulars ~9–11/hit, boss ~17. `TEST_BALANCE=1` dumps enemy damage per floor for tuning. Knob: `baseAtk` in `StartNewFloor`. *Possibly slightly hot given floor 1 spawns 4 regulars + a boss — pending owner's eyeball.*

**2026-07-24 — Enemies are now full characters (data-driven generation).** Replaced the hand-tuned enemy stat block with `EnemyFactory`: each enemy gets a **race + class + level**, from which stats are derived (class starting distribution + per-level growth + small even growth, then scaled by race effectiveness and baked into the stored stats) and gear is assigned (class starting loadout → `CurrentAttack`). Damage flows from the equipped attack + effective stats (physical/magic/faith), and the projectile uses that attack's visual — so a Mage enemy lobs a magic bolt, an Archer shoots arrows. Weighted class selection (`RegularClassWeights`); bosses use `BossClassWeights` (rarer classes), sit at the top of the floor's level range (`LevelRange(floor) = (floor, floor+2)`, boss = max+1), get ~2.2× HP and a 1.35× damage floor. Renderer maps class → shape/color (`ClassColor`/`ClassShape`). `TEST_BALANCE=1` dumps per-floor class/race/level/damage. Build clean, sim 17/17.
- **Balance pass v1:** magic/faith scaling brought down to the physical rate (1.5→1.2) so casters aren't disproportionate; boss damage floor added.
- **Known balance concerns (need owner's eyeball/steering):** (1) casters still scale steeply at high level — a level-8 Mage boss can hit ~87 vs a level-1 hero (less scary vs a leveled hero, but an outlier); (2) boss threat varies a lot by class since bosses favor rare classes and Bard/Priest are low-damage support (a Bard boss ~14, a Mage boss ~87). Levers: `LevelRange` (per-floor difficulty), the 1.2 scaling coefficients, `BossClassWeights` (favor threatening classes), and enemy HP/attack derivation in `EnemyFactory`.

**2026-07-24 — Balance decision (owner): keep caster artillery spikes, make bosses consistently threatening.** Added a level-scaled boss damage floor (`max(1.35x, 12 + level*2)`) so a low-damage support-class boss (Bard/Priest) can't be a pushover, while Mage bosses still spike well above it untouched (~87 at level 8, unaffected — the intended exception). Verified: floor-1/3 Bard bosses rose from ~14–16 to a consistent ~20–24; sim still 17/17.

**2026-07-24 — Phase 1 CLOSED OUT.** All three remaining items done and verified (build clean; `TEST_SIM` 17/17; new `TEST_CODEX=1` demo confirms tier distribution + a real auto-play sim producing consistent, internally-verified Codex output — e.g. Boss `HP 140 → XP 90` matches `(10+140/4)×2.0` exactly).
- **Monster tiers + xlsx XP**: added `EnemyTier` (Basic/Elite/Boss) to `Enemy` (`IsBoss`/`IsElite` kept as computed properties, so existing boss-only code didn't need to change). `EnemyFactory.RandomRegular` rolls an 18% Elite chance (1.5× HP, distinct radius); kill XP now applies `Enemy.XpMultiplier` (Basic 1x / Elite 1.5x / Boss 2x, from the xlsx Monster Tier table) on top of the unchanged baseline formula, so Basic-tier rewards are identical to before.
- **Enemy loot drops**: `GameState.HandleEnemyDefeated` (the single choke point where an enemy's HP reaches zero) now also rolls a tier-scaled loot chance (Basic 15% / Elite 45% / Boss 100%) through the existing `LootService`/`AcquireLoot` pipeline — "find gear from enemies" now works, not just chests.
- **Codex**: new `Core/Models/CodexData.cs` + `Core/Services/CodexService.cs`, directly modeled on the SimpleRPG `MetaManager` pattern (see prior-attempt reference below) — a bestiary keyed by `"{Race} {Class}"` (seen/killed/first-last-floor) plus cross-run play stats (kills, deaths, deepest floor, floors cleared), persisted as JSON to `Saves/codex.json` (new, gitignored — player save data, not build output). `RecordEncounter` fires on first combat engagement (in-memory only, since it can fire often); `RecordKill`/`RecordDeath`/`RecordFloorCleared` are rare events that flush to disk. `StatsOverlay` shows a minimal CODEX section (kills/deaths/deepest floor/bestiary discovery count) — full per-species browsing is left for later, per the original "even if the UI is minimal" scope.
- **Renderer**: Elite/Boss enemies get a distinguishing halo ring (silver/gold) on top of their class shape.
- One decision made along the way: enemy `IsBoss`/`Tier` fields required touching `CombatSystem`'s boss-damage-floor code — no changes needed there since it already read `enemy.IsBoss`, now a computed property.

**Phase 1 exit criteria met**: `Combinable` model + merge engine exist and are verified; attacks are data-driven from equipped gear with stable ids; racial effectiveness matches the owner's full spec; inventory/hotbar equip works; enemies are generated as full characters (race/class/level/tier) with tiered XP and loot; the Codex records discovery and run history. **Next: Phase 2** — wire `CombinationEngine` to in-world Forge/Shrine features + a combine UI (the signature mechanic, still the single biggest gap vs. both north-star docs and the SimpleRPG prior attempt).

**2026-07-24 — Dungeon restructure (first concrete piece of the north-star pivot, section 0a).** Regular-floor pacing overhauled and the safe-room/Guardian gate built and verified. Build clean; full regression suite intact (`TEST_SIM` 17/17, `TEST_COMBINE`/`TEST_LOOT`/`TEST_BALANCE`/`TEST_CODEX` all still pass); new `TEST_DUNGEON=1` demo directly verifies every new mechanic.
- **No more per-floor boss or key gate.** `StartNewFloor` no longer spawns a boss into the regular maze, and chests are loot/XP-only (no key). Stairs are usable the instant the hero reaches them — the challenge is genuinely solving the maze, not a fetch-quest.
- **Stairs placed genuinely far from the entrance.** New `Maze.BfsDistancesFrom(x,y)` computes real maze-graph distance (not straight-line); stairs are picked from the farthest-reachable quartile. Verified via `TEST_DUNGEON`: BFS distances of 200–400+ steps on a 41×31 maze — a real traversal challenge, not a coin flip.
- **Safe room every 4 floors** (`FloorsPerGate = 4`), entered via `EnterSafeRoom()`/`GenerateSafeRoomMaze()` — a small (15×9), fully-lit, hand-built room (not run through the normal maze generator), with a `Shrine` feature (faint blue glow, center) and a `GuardianDoor` feature (far wall). `IsInSafeRoom` gates a 3x regen-rate buff (the "restful effect") on top of the existing tick-rate-derived regen math. Verified via teleport-based `TEST_DUNGEON` checks (see below).
- **Guardian encounter**: approaching the `GuardianDoor` calls `SpawnGuardian`, reusing the exact same `EnemyFactory.RandomBoss` generation the old per-floor boss used — a Guardian *is* the dungeon's boss concept, just relocated to its own room. Depth-scaling is automatic (same formula, fed the current `CurrentFloor`), so deeper gates are naturally tougher with no extra code. Defeating it (`HandleEnemyDefeated`) proceeds straight to the next floor group.
- **Shrine exit hook** (`ExitDungeonToOverworld`): since the Overworld doesn't exist yet, this is an explicit, clearly-marked **placeholder** — it preserves the hero completely (level/XP/gear unchanged, unlike death which resets everything) and starts a fresh dungeon dive with a new seed. Verified: a hero manually leveled to 3 (XP 75) kept exactly that after touching the shrine. A new `CodexService.RecordDungeonExit`/`PlayStats.TotalDungeonExits` tracks these separately from deaths. **This hook is exactly where the real Overworld hand-off plugs in later.**
- **First environmental hazard**: a one-shot `Trap` feature (unmitigated burst damage, `8 + floor*2`, plus a hit-flash and screen-shake bump), placed on ~40% of regular floors. Verified via `TEST_DUNGEON` (a hero stepped on one for 10 damage on floor 1, matching the formula exactly).
- **Autonomous safe-room behavior is a known, flagged placeholder**: with no player-input system yet, auto-play defaults to *always* pushing toward the Guardian door rather than the shrine (pursuing the existing hero's "always push forward" character). This is a real design gap, not a considered choice — the shrine-vs-Guardian fork is exactly the kind of decision that should become genuine player input once the pause/turn-based-mode feature (section 0a) exists.
- **A real bug found and fixed along the way**: Guardian defeat originally called `StartNewFloor()` (which clears `Enemies`/`Projectiles`) directly from inside `HandleEnemyDefeated` — but that method is invoked from *within* the live `foreach (var p in Projectiles)` loop in `ProcessProjectileCollisions`, so mutating those collections mid-enumeration crashed with `InvalidOperationException`. Fixed by deferring the transition via a `_pendingGuardianVictory` flag, applied in `Tick()` right after `UpdateProjectilesAndEffects()` completes (outside any enumeration). The `ExitDungeonToOverworld`/`AdvancePastFloor` call sites didn't have this problem — they're called from `CheckFeatures()`'s `.ToList()`-copied iteration, which is safe to mutate the source collection under.
- **Honest balance observation, not yet acted on**: in an organic ~11-minute auto-play run (`TEST_DUNGEON`'s first section), the hero died repeatedly and never once reached floor 4's safe room organically — the combination of far-apart stairs (a real, large traversal) and existing combat difficulty makes early floors slow and dangerous. Worth an eyeball pass once played directly; the levers are `LevelRange`/enemy difficulty (existing) and how "far" the far-stairs quartile is (`StartNewFloor`'s `candidates.Count / 4`).
- **Deferred to a follow-up pass** (explicitly scoped out of this session, not forgotten): **varied room/area sizes** within regular floors (the maze is still a uniform recursive-backtracker corridor maze — SimpleRPG's BSP-room approach with rooms of size 4–10 is the reference point) and the **locked-door + monster-key vault** (an occasional side-room gated by a door whose key drops from one specific designated enemy on that floor, not a chest). Both are real, well-scoped features; they just involve enough design surface (connectivity-safe room carving; picking/tracking a "key holder" enemy) that they deserve their own focused pass rather than being rushed alongside everything above.

---

## Reference: prior attempt at this game (`D:\Code\SimpleRPG`)

A separate, complete prior attempt at the same Game Idea.md vision — vanilla JS/Canvas, turn-based ASCII-roguelike (not real-time/idle), BSP dungeons, shadowcasting FOV. Reviewed 2026-07-24 for visual/feel inspiration and reusable patterns. Its own `Implementation Status.md`/`Project Pieces.md` independently name **item/spell/ability combining as the single biggest missing feature**, never built there either — useful confirmation that Phase 2 deserves real care. Its class/race stat tables use the *exact same* multiplicative values as our Racial Effectiveness spec, confirming that's a settled cross-project constant.

**Adopted (2026-07-24):**
- **Camera clamped to maze bounds** — the centering point (not the lerp state) is clamped so the viewport never scrolls past the edge into void; the lerp state keeps tracking the hero exactly so there's no edge-sticking/jump artifact.
- **Screen shake on hero hits** — `GameState.ScreenShake`, set on hero damage (scaled to damage/maxHP, capped at 5px) and decayed 20%/tick; renderer applies it as camera jitter.
- **Low-HP red vignette** — creeps in under 50% HP, matching their `t = 1 - hpRatio/0.5` formula. `MazeRenderer.DrawLowHealthVignette`.

**Not adopted / noted for later:**
- Their monster XP (flat depth-scaled per-monster value) is simpler than what we built — xlsx-tiered XP is still the right target for our open item #1.
- A ready-made **elemental status-effect table** (Fire→Burn, Ice→Freeze, Lightning→Paralysis, Poison→DOT, Earth→Stun, Air→Blind, Water→Slow) — relevant once combining produces elemental gear (`GameAttribute` has these elements with no behavior yet). Candidate for Phase 2/3.
- Their `MetaManager` bestiary (`recordEncounter`/`recordKill`, seen/killed/first-last-floor, versioned+debounce-saved to localStorage) is a clean **pattern** for our Codex + end-of-run stats (open items #2/#3) — port the shape to a C# `SaveService` writing JSON, not the code itself.
- Per-weapon melee FX (sword arc / mace shockwave ring / dagger thrust+afterimage / unarmed starburst) confirmed our "distinct effect per attack" approach is right; their mace shockwave ring is a shape we don't have (ours defaults Heavy to a generic thick line) — candidate for a future visual pass.
- Class evolution tree screenshot (`Ideas/Screenshot ...png`) shows a more refined version of the same tree in our own `Game Class and Stats.drawio.xml` (same class names — Reaper, Paragon of Light/Darkness, Archmage, etc.) — good additional reference for Phase 4, not actionable now.

---

## 1. Current state at a glance

**Tech:** .NET 9, Avalonia UI + Skia canvas, single project. Builds clean (`dotnet build` → 0 warnings / 0 errors). ~5,600 lines of C#. Headless test mode via `TEST_SIM=1` env var runs a race/class matrix and exits.

### What works and is faithful to the design
- **Procedural mazes** — correct recursive-backtracker DFS, always fully connected (`Core/Systems/MazeGenerator.cs`).
- **Auto-play loop** — BFS exploration, enemy pursuit, return-to-stairs; vision-cone + Bresenham line-of-sight aggro gating for hero and enemies; unified "everything is a short-lived projectile" hit model (even melee). Coherent and well-built for its size.
- **7 classes × 7 races** load from JSON and apply. **The class starting-stat table in `Data/Classes/classes.json` matches the spreadsheet exactly** — all six classes sum to the designer's 28-point base. This is a genuine point of fidelity.
- **Combat depth** — resource-gated heavy attacks (Stamina/Mana/Faith), crit chance, knockback fields, per-class enemy archetypes (Brute/Striker/Archer/Caster) with distinct stats, shapes, and colors.
- **Rendering** — smooth camera lerp, bespoke per-attack particle effects, chest-opening animation with rising key, HP/resource/XP bars, debug hitbox/LOS overlays (`DEBUG_HITBOXES` / `DEBUG_LOS` env vars).
- **Leveling / death loop** — XP on kill, per-class stat growth, milestone attack unlocks (L5/10/15), death overlay with auto-restart.

### What is stubbed, thin, or absent (vs. the dream)
| Dream system (source) | Status in code |
|---|---|
| Items / equipment / inventory / rarities / attributes | **No `Item` model exists at all** |
| Spells as a concept distinct from `Attack` | Absent — everything is a hardcoded `Attack` |
| Abilities / passives (start with 1, unlock more) | Absent |
| **Merge / combine system** (the signature mechanic) | **Zero code** |
| Class & species evolution trees | Absent (design only — see [Class Tree Inventory.md](Class%20Tree%20Inventory.md), the authoritative 115-class breakdown) |
| Character builder depth (stat points, ability picks, save slots/templates) | Only name + race pick + class pick |
| Persistence (run history, unlocked library/codex, seeds) | None |
| End-of-run stats screen (kills/damage) | None — death screen shows a name + timer only |
| Boss-gated portal progression | Not wired — floor advance is chest-key only |
| NPCs / towns / merchants / trainers / followers | Absent |
| Environment effects, endless mode, village-defense mode | Absent |

---

## 2. Design decisions that must be made before building

These are genuine forks where the two north-star docs disagree or under-specify. Pick deliberately; each unblocks a chunk of the plan.

1. **Stat model: 10-point allocation vs. 28-by-class.**
   `Game Idea.md` §Stats says "start at 1, distribute 10 points." The xlsx says "28 base stats, distribution by class" (and the code implements this faithfully).
   *Recommendation:* keep the 28-by-class base (already built and matches the sheet) **and** layer an optional small allocation pool on top during character creation for the Game Idea.md flavor. Reconciles both.

2. **Race modifiers: additive vs. multiplicative.**
   Code applies flat integer bonuses (`Data/Races/races.json`, e.g. Elf Agility +2). The xlsx explicitly specifies **multipliers, rounded** — with a worked example: "a 4 in strength for a human means 4, but for a dwarf means 5.2 → 5." The multipliers are already tabulated in the sheet (Human 1.0, Dwarf Str/Con 1.3, Elf Int 1.25 / Agi 1.2, Goblin Cha 0.4, …).
   *Recommendation:* switch to the multiplicative model — it's a small, well-specified data+formula change and makes races actually matter.

3. **Race roster mismatch.**
   Code races: Human/Elf/Dwarf/Halfling/**Orc/Tiefling/Dragonborn**. xlsx races: Human/Elf/Dwarf/Halfling/**Goblin/Kobold**. Decide the canonical roster (the drawio has lore for Goblins specifically, incl. a reputation/HERO-tag mechanic).

4. **XP curve.**
   Code is exponential (`ExperienceToNext *= 1.5`, `Core/Models/Hero.cs:98`). The xlsx tabulates quadratic candidates (100·L² → 100/400/900/1600…) out to level 65, plus two alternatives.
   *Recommendation:* adopt the quadratic curve from the sheet; it's a one-line change and makes the authored XP tables meaningful.

5. **Attack identity.**
   Attacks are currently matched by **substring on their display name** for both visuals and special behavior (e.g. arrow art only renders for names containing "Bow"/"Arrow" — so Archer's "Quick Shot" silently loses its art; "Arcane Blast" radius growth is independently string-matched in two files). This must move to stable IDs/tags before attacks become data-driven (see §4). Decide the ID scheme now.

---

## 3. Fix cluster — do this first (Phase 0)

Small, contained, high-value. These corrupt balancing and hide behind a confusing clock, so clear them before building anything new. All are verified against current `HEAD` and the headless sim.

| # | Issue | Location | Fix | Size |
|---|---|---|---|---|
| 0.1 | **Whole game runs ~3× too fast.** Loop ticks at 30/s but every duration constant & rate is written for 10/s and never rescaled. Chests open in 1s not 3s, auto-restart in 1.67s not 5s, regen & movement ~3× fast. | `ViewModels/MainWindowViewModel.cs:28`; constants in `Core/Services/GameState.cs:33,48`, `Core/Models/Hero.cs:73`; regen `GameState.cs:155-194` | Pick one tick rate, **wire in `Data/Config/settings.json` (currently unused, already says `tickRate:30`)**, and derive tick counts from seconds so they can't drift again. | S |
| 0.2 | **Death countdown displays wrong time.** Shows "5.0s" but elapses in 1.67s. | `UI/Controls/DeathOverlay.axaml.cs:89` (`/ 10f`) | Derive from the real tick rate (fixes automatically once 0.1 lands). | XS |
| 0.3 | **Free instant first-strike for ranged/casters.** `Hero.InCombat`/enemy flags are set *before* the guard that would call `StartCombat`, so `StartCombat` (which grants a 3-tick wind-up) never runs. Magic Dart fires at tick 0 every run. | `Core/Services/GameState.cs:396-415`; `Core/Systems/CombatSystem.cs:402-411` | Track "just entered combat" before mutating flags, or call `StartCombat` from the transition. | S |
| 0.4 | **Dead kill/XP block.** Damage is projectile-deferred, so `if (!enemy.IsAlive)` right after `PerformHeroAttack` is always false — its XP award, flag reset, and global `projectiles.Clear()` never run. (Live XP path is `GameState.cs:493`.) | `Core/Systems/CombatSystem.cs:84-98` | Delete the dead block; keep XP in the projectile-collision path. | XS |
| 0.5 | **Off-by-one floor.** `CurrentFloor` starts at 1 then `StartNewFloor()` pre-increments, so play begins on "Floor 2" (one floor of scaling too hard). | `Core/Services/GameState.cs:38,967` | Init to 0, or don't pre-increment on first call. | XS |
| 0.6 | **Debug spam.** `Console.WriteLine` fires every combat tick, ungated. | `CombatSystem.cs`, `GameState.cs`, `CharacterDataService.cs` | Gate behind a `DEBUG_*` flag like the existing overlay toggles, or a logging shim. | S |
| 0.7 | **Dead code removal.** `GameState.GetAttackSpeedForType()` (no callers), `GameState.Reset()` (no callers), `MazeTest.cs` (whole class, no callers), `reservedCells` (`GameState.cs:993`, written never read), `Boss` property (written never read), `Data/Classes/wanderer.json` (wrong schema, never loaded), `MazeFeatureType.Shrine` (never placed). | various | Delete or, for `Boss`/`Shrine`, wire them up in later phases (see §5). | S |
| 0.8 | **`readonly` fields mutated via reflection** in `RestartGame()`. Works but is a smell. | `Core/Services/GameState.cs:922-930` | Drop `readonly`, assign directly. | XS |
| 0.9 | **Stale committed logs** `playtrace.txt` / `playtrace_fixed.txt` in repo root don't reflect current behavior. | repo root | Delete and gitignore. | XS |

**Exit criteria for Phase 0:** one authoritative tick rate wired from settings; combat wind-up consistent for all classes; dead paths gone; headless sim re-run and reviewed; balancing can now be trusted.

---

## 4. The keystone: a unified "Combinable" model

Everything that makes the dream *this game* — items, spells, abilities, and the merge system that ties them together — depends on one architectural decision. `Game Idea.md` says spells combine with spells/items/abilities, items with items/spells/abilities, abilities with abilities/spells. That is impossible to build cleanly if items/spells/abilities are separate hardcoded types. Build a common substrate **first**.

**Proposed shape (Phase 1–2):**
- `Combinable` (base/interface): `Id`, `Name`, `Kind` (Weapon / Item / Spell / Ability / Armor), `Rarity` (Common→Mythic), `Attributes` (a set of tags: Fire, Ice, Lightning, Shadow, Holy, Poison, Arcane, Sharp, Heavy, Light, Fragile, Cursed, Blessed…), `Level`, and effect payload (damage, scaling stat, cooldown, resource cost, on-hit effects).
- `CombinationEngine`: `Combine(a, b, contextLocation) → Combinable`, applying the rules from `Game Idea.md`:
  - same attribute → **intensify** (Fireball + Fireball → Fireball+),
  - different attributes → **new blended attribute** (Fire + Ice → Frostfire; Fire + Shadow → Infernal special-case),
  - cross-kind → **new kind** (Bow + Fireball → Fire Bow *item*; Fireball + Dense Musculature → Fire Fist *ability*),
  - **rarity math** (point-value model from the doc: common+rare → uncommon),
  - **level math** (result at lower of the two; a rarity increase resets to level 0),
  - **location gating** (spells anytime; weapons only at anvil/forge; abilities only at a shrine).
- **`Attack` becomes a runtime projection** of an equipped Weapon/Spell/Ability rather than a hand-authored `AttackFactory` switch. This is also what retires the fragile name-substring matching (§2.5): projections carry stable IDs/tags that drive both combat behavior and rendering.
- `DiscoveryRegistry` / **Codex** — records every base and combined entity the player has discovered, with descriptions; persists across runs (the "library of items and spells" unlockable).

Get this right and items, spells, abilities, merging, and the codex all fall out of one system. Get it wrong (separate silos) and every later phase fights the architecture.

---

## 5. Phased roadmap to the dream state

Dependency-ordered. Each phase is shippable and playable. Sizes are rough: S (hours), M (days), L (1–2 wks), XL (multi-wk).

### Phase 0 — Foundation fixes & cleanup  *(see §3)*  — **S–M total**
Clock, combat wind-up, dead code, debug gating, settings wired. Nothing new; everything after this is built on a trustworthy base.

### Phase 1 — Data-model foundation  — **L**
*Goal: the nouns of an RPG exist as data, and xlsx numbers are honored.*
- Introduce `Combinable` base + `Item`, `Spell`, `Ability` deriving from it (§4). Don't build combining yet — just model + equip + use.
- Refactor `AttackFactory`'s hardcoded switch into **data-driven attacks projected from equipped Weapon/Spell** (stable IDs/tags; kills the substring matching, fixes Archer art as a side effect).
- Basic **inventory + equip** (a few hotbar-style slots per `Game Idea.md`'s "key item slots").
- Align to xlsx: **quadratic XP curve** (decision 2.4), **multiplicative race modifiers** (decision 2.2), **monster tiers** (Basic/Elite/Boss with the sheet's XP rewards) replacing `10 + MaxHp/4`.
- **Codex data structure** (discovery list) even if the UI is minimal.
- *Touchpoints:* new `Core/Models/Item.cs`, `Spell.cs`, `Ability.cs`, `Combinable.cs`; rewrite `Core/Services/AttackFactory.cs`; `CharacterDataService` race math; `Hero` leveling; `GameState` enemy spawn + XP.

### Phase 2 — The merge / combine system  — **L–XL**  *(the signature feature)*
*Goal: the mechanic the whole design is built around.*
- `CombinationEngine` with the full rule set (§4): same/different attribute, cross-kind, rarity math, level math.
- **Combine locations** in the maze: reuse the dormant `MazeFeatureType.Shrine` (abilities) and add anvil/forge (weapons); spells combine anytime.
- Result discovery → Codex; combined entities **persist** (they're usable in future runs per the doc).
- A **combine UI** (even simple: pick two, preview result, confirm/swap/inventory per the "swap, combine, or send to inventory" flow).
- Seed the recipe space with the doc's worked examples (Frostfire, Fire Bow, Bubble Bow, Fire Fist, Fire Affinity, Holy Flame, Rapier, Sniper…) as validation cases.
- *Depends on:* Phase 1 model. *Unblocks:* most of the "unlockables" fantasy.

### Phase 3 — Character builder & progression depth  — **M–L**
*Goal: the pre-run builder and in-run growth match the doc.*
- Character creation: **stat-point allocation** (decision 2.1), **ability selection** (max by species), **save slots / templates** ("save character as template, adjust in future games").
- **Save system** (JSON under a user profile dir): templates, unlocked races/classes/abilities, Codex, run history. Cross-cutting; introduced here, extended later.
- **Spells/abilities level with use**; player levels with kills (per doc); spell evolution choices on level-up.
- *Touchpoints:* `Views/CharacterSelectWindow.*` (major expansion), new `Core/Services/SaveService.cs`, `Hero`/`Spell`/`Ability` leveling.

### Phase 4 — Run structure, meta & unlockables  — **L**
*Goal: a roguelite loop with persistence and reasons to replay.*
- **Boss-gated portal progression**: boss defeat unlocks the stairs/portal (wire up the currently-write-only `Boss` property). Chest→key can remain a secondary gate or become loot.
- **End-of-run stats screen** (kills, damage dealt/taken, floors, loot score) — replaces the bare death overlay; both north-star docs call for it.
- **Unlock system**: races (Human→Manaborn line), classes, abilities unlocked through play; **class & species evolution** — see [Class Tree Inventory.md](Class%20Tree%20Inventory.md) (authoritative, 2026-07-24): 6 starter Commons → 11 action-unlocked Common specializations (e.g. "punch an enemy to death" → Monk, "kill an enemy with a sword" → Spellsword) → level-gated Uncommon/Rare/Epic/Legendary evolutions at 10/25/50/75 → Mythical. Families branch and share nodes across starter classes (Duelist bridges Rogue/Warrior, Paladin bridges Warrior/Priest, Monk bridges Priest/Wanderer); Mage Apprentice's Elementalist line further splits into 6 elemental sub-trees. Implementation-wise this is a big content/data-authoring lift once Phase 1's `Combinable`/class model stabilizes — plan to model it as data (per-class unlock rules + evolution tables), not hardcoded branches in C#.
- **Endless mode** after the final floor.
- *Depends on:* save system (Phase 3), items/abilities (Phase 1–2).

### Phase 5 — World & NPCs  — **XL**
*Goal: the living-world layer.*
- **NPCs / towns / merchants / trainers**, scaling down in frequency by floor (per doc).
- **Followers** (Charisma-gated) and follower buffs.
- **Environment / chunk effects** (fire weak in rain, water weak in volcano).
- **Village-defense mode** (the xlsx/drawio "monsters mass at a portal and march on a village; keep a population % alive").
- Reputation / alignment (Goblin HERO-tag mechanic; evil vs. good branches feeding Phase 4 evolutions).
- Largest and least-specified; revisit scope when Phases 1–4 land.

---

## 6. Suggested near-term sequence (the next few PRs)

1. **PR 1 — Clock & combat correctness** (0.1–0.5): tick rate from settings, wind-up fix, dead-block delete, floor off-by-one. Re-run headless sim; confirm behavior.
2. **PR 2 — Cleanup** (0.6–0.9): debug gating, dead-code removal, reflection removal, stale-log deletion.
3. **PR 3 — Decisions locked** (§2): write the chosen stat model, race math, roster, XP curve, and attack-ID scheme into this doc. No code needed to *decide*; small code to apply XP curve + race multipliers.
4. **PR 4 — `Combinable` foundation** (Phase 1 start): the base model + data-driven attacks. This is the architectural fork; worth reviewing carefully before the merge engine builds on it.

Phases 0–1 make the existing game *correct and data-driven*; Phase 2 makes it *the dream's game*; Phases 3–5 make it *deep*. The single highest-leverage decision is §4 — build the unified `Combinable` substrate before any of items/spells/abilities/merging, or every later phase pays interest.

---

## 7. Appendix — verification basis

Findings here were verified against current `HEAD`: full source read (~5,600 LOC), clean `dotnet build`, live `TEST_SIM=1` headless runs across the race/class matrix, and grep-confirmed call-site analysis for every "dead code" / "never read" claim. The xlsx and drawio were extracted and parsed directly (not just filenames). The one item the first pass flagged as a possible bug — the negative-HP snapshot in the old `playtrace.txt` — was reproduced live and **cleared as a false alarm** (normal post-kill snapshot; killing blows aren't HP-clamped to 0, which is cosmetic).
